apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-image
spec:
  serviceName: postgres-image-service
  replicas: 1
  selector:
    matchLabels:
      app: postgres-image
  template:
    metadata:
      labels:
        app: postgres-image
    spec:
      containers:
      - name: postgres
        image: postgres:latest
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: image_db
        - name: POSTGRES_USER
          value: myuser
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: DATASOURCE_PASSWORD
        volumeMounts:
        - name: postgres-image-storage
          mountPath: /var/lib/postgresql/data
        readinessProbe:
          exec:
            command: ["pg_isready", "-U", "myuser", "-d", "image_db"]
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          exec:
            command: ["pg_isready", "-U", "myuser", "-d", "image_db"]
          initialDelaySeconds: 15
          periodSeconds: 20
      volumes:
      - name: postgres-image-storage
        persistentVolumeClaim:
          claimName: postgres-image-pvc