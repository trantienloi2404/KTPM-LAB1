apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-authenticate-pvc
  namespace: microservices-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-authenticate
  namespace: microservices-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-authenticate
  template:
    metadata:
      labels:
        app: postgres-authenticate
    spec:
      containers:
      - name: postgres
        image: postgres:latest
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: "authenticate_db"
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: POSTGRES_PASSWORD
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - myuser
            - -d
            - authenticate_db
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - myuser
            - -d
            - authenticate_db  
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: postgres-authenticate-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-authenticate-service
  namespace: microservices-app
spec:
  selector:
    app: postgres-authenticate
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP 